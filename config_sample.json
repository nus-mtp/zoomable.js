{
	"database": {
		"postgresql": {
			"adapter": "sails-postgresql",
			"host": "YOUR_POSTGRESQL_HOSTNAME",
			"user": "YOUR_POSTGRESQL_USER",
			"password": "YOUR_POSTGRESQL_PASSWORD",
			"database": "YOUR_POSTGRESQL_DATABASE"
		}
	}
}